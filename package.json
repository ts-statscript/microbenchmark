{
  "name": "@ts-statscript/microbenchmark",
  "version": "0.0.2",
  "main": "./dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/ts-statscript/microbenchmark.git"
  },
  "author": {
    "name": "Dereck Mezquita",
    "email": "dereck@mezquita.io"
  },
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "prepublish": "npm run build",
    "prepublishOnly": "npm run build",
    "publishAlpha": "npm version prerelease --preid=dev --no-git-tag-version && npm build && npm publish --tag dev --non-interactive",
    "format": "prettier --write .",
    "test": "jest"
  },
  "prettier": {
    "semi": true,
    "trailingComma": "none",
    "singleQuote": true,
    "printWidth": 80,
    "tabWidth": 4,
    "useTabs": false,
    "overrides": [
      {
        "files": "*.yml",
        "options": {
          "tabWidth": 2
        }
      }
    ]
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.26",
    "husky": "^9.1.2",
    "jest": "^29.7.0",
    "prettier": "^3.3.3",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.2"
  }
}
